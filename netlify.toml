[build]
  publish = "public"
  edge_functions = "netlify/edge-functions"

# No [[redirects]] block for /api/chat — the edge function claims that path
# itself via `export const config = { path: "/api/chat" }`.
